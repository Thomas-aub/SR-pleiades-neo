# =============================================================================
# requirements.txt — SR-Pléiades NEO
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# PyTorch is NOT listed here because the correct version depends on your CUDA
# driver. Install it first from https://pytorch.org/get-started/locally/
# Example (CUDA 12.4):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# =============================================================================

# ── Geospatial / raster I/O ───────────────────────────────────────────────────
rasterio>=1.3.0          # GeoTIFF read/write for all pipeline stages

# ── Scientific computing ──────────────────────────────────────────────────────
numpy>=1.26.0            # Array ops used throughout

# ── Deep learning utilities ───────────────────────────────────────────────────
timm>=0.9.0              # Vision model building blocks — pulled in by KAIR/SwinIR

# ── Config & serialisation ────────────────────────────────────────────────────
pyyaml>=6.0              # YAML config parsing (preprocessing.yaml, swinir_finetune.yaml, inference.yaml)

# ── Progress bars & logging ───────────────────────────────────────────────────
tqdm>=4.65.0             # Progress bars in preprocessing, training, and inference

# ── Training monitoring (optional but recommended) ────────────────────────────
tensorboard>=2.14.0      # TensorBoard scalar logging
                         # Launch with: tensorboard --logdir runs/swinir_finetune/tensorboard

# ── Perceptual loss (optional) ────────────────────────────────────────────────
# Required only when loss.perceptual.enabled: true in swinir_finetune.yaml
torchvision>=0.18.0      # VGG feature extractor for perceptual loss
                         # Also needed if PyTorch was installed without it

# ── GPU-accelerated preprocessing (optional) ─────────────────────────────────
# Required only when use_gpu: true in preprocessing.yaml (pansharpening / degradation)
# Install the build matching your CUDA version:
#   CUDA 11.x:  pip install cupy-cuda11x
#   CUDA 12.x:  pip install cupy-cuda12x
# cupy-cuda12x